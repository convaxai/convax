name: Publish Marketplace packages

on:
  workflow_dispatch:
    inputs:
      commit:
        description: Exact main commit to stage
        required: true
        type: string
      marketplace_version:
        description: Expected @convax/marketplace version
        required: true
        default: 0.2.2
        type: string
      kit_version:
        description: Expected @convax/marketplace-kit version
        required: true
        default: 0.2.3
        type: string
      sdk_version:
        description: Required published @convax/plugin-sdk version
        required: true
        default: 0.5.0
        type: string
      create_version:
        description: Expected create-convax-marketplace version
        required: true
        default: 0.2.3
        type: string

permissions:
  contents: read
  id-token: write

concurrency:
  group: publish-marketplace-packages
  cancel-in-progress: false

jobs:
  stage:
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803
        with:
          ref: ${{ inputs.commit }}
          fetch-depth: 0
      - name: Verify protected source identity
        env:
          EXPECTED_COMMIT: ${{ inputs.commit }}
        run: |
          set -euo pipefail
          [ "$(git rev-parse HEAD)" = "$EXPECTED_COMMIT" ]
          git merge-base --is-ancestor HEAD origin/main
      - uses: actions/setup-node@249970729cb0ef3589644e2896645e5dc5ba9c38
        with:
          node-version: 24
          registry-url: https://registry.npmjs.org
      - uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6
        with:
          bun-version: 1.3.14
      - run: npm install --global npm@11.18.0
      - run: bun install --frozen-lockfile --ignore-scripts
      - run: bun check
      - name: Pack and verify immutable tarballs
        env:
          MARKETPLACE_VERSION: ${{ inputs.marketplace_version }}
          KIT_VERSION: ${{ inputs.kit_version }}
          CREATE_VERSION: ${{ inputs.create_version }}
          SDK_VERSION: ${{ inputs.sdk_version }}
        run: |
          set -euo pipefail
          mkdir -p "$RUNNER_TEMP/npm-release"
          (cd packages/marketplace && bun pm pack --destination "$RUNNER_TEMP/npm-release")
          (cd packages/marketplace-kit && bun pm pack --destination "$RUNNER_TEMP/npm-release")
          (cd packages/create-convax-marketplace && bun pm pack --destination "$RUNNER_TEMP/npm-release")
          marketplace_tarball="$RUNNER_TEMP/npm-release/convax-marketplace-$MARKETPLACE_VERSION.tgz"
          kit_tarball="$RUNNER_TEMP/npm-release/convax-marketplace-kit-$KIT_VERSION.tgz"
          create_tarball="$RUNNER_TEMP/npm-release/create-convax-marketplace-$CREATE_VERSION.tgz"
          [ -f "$marketplace_tarball" ]
          [ -f "$kit_tarball" ]
          [ -f "$create_tarball" ]
          [ "$(tar -xOf "$marketplace_tarball" package/package.json | jq -r '.name + "@" + .version')" = "@convax/marketplace@$MARKETPLACE_VERSION" ]
          [ "$(tar -xOf "$kit_tarball" package/package.json | jq -r '.name + "@" + .version')" = "@convax/marketplace-kit@$KIT_VERSION" ]
          [ "$(tar -xOf "$create_tarball" package/package.json | jq -r '.name + "@" + .version')" = "create-convax-marketplace@$CREATE_VERSION" ]
          [ "$(tar -xOf "$kit_tarball" package/package.json | jq -r '.dependencies["@convax/marketplace"]')" = "^$MARKETPLACE_VERSION" ]
          [ "$(tar -xOf "$kit_tarball" package/package.json | jq -r '.dependencies["@convax/plugin-sdk"]')" = "^$SDK_VERSION" ]
          [ "$(tar -xOf "$create_tarball" package/package.json | jq -r '.dependencies["@convax/marketplace-kit"]')" = "^$KIT_VERSION" ]
          ! tar -xOf "$kit_tarball" package/package.json | grep -Eq 'workspace:|catalog:'
          ! tar -xOf "$create_tarball" package/package.json | grep -Eq 'workspace:|catalog:'
          sha512sum "$marketplace_tarball" "$kit_tarball" "$create_tarball" > "$RUNNER_TEMP/npm-release/SHA512SUMS"
      - name: Refuse version reuse
        env:
          MARKETPLACE_VERSION: ${{ inputs.marketplace_version }}
          KIT_VERSION: ${{ inputs.kit_version }}
          CREATE_VERSION: ${{ inputs.create_version }}
        run: |
          set -euo pipefail
          assert_version_available() {
            local package="$1"
            local version="$2"
            local output
            if output="$(npm view "$package@$version" version --json 2>&1)"; then
              echo "$package@$version already exists and cannot be reused" >&2
              exit 1
            fi
            if [[ "$output" != *"E404"* ]]; then
              echo "Could not safely verify $package@$version availability" >&2
              printf '%s\n' "$output" >&2
              exit 1
            fi
          }
          assert_version_available "@convax/marketplace" "$MARKETPLACE_VERSION"
          assert_version_available "@convax/marketplace-kit" "$KIT_VERSION"
          assert_version_available "create-convax-marketplace" "$CREATE_VERSION"
      - name: Require the Plugin SDK dependency to be published first
        env:
          SDK_VERSION: ${{ inputs.sdk_version }}
        run: |
          set -euo pipefail
          [ "$(npm view "@convax/plugin-sdk@$SDK_VERSION" version --json | jq -r .)" = "$SDK_VERSION" ]
      - name: Stage packages for maintainer approval
        env:
          NPM_CONFIG_PROVENANCE: "false"
          MARKETPLACE_VERSION: ${{ inputs.marketplace_version }}
          KIT_VERSION: ${{ inputs.kit_version }}
          CREATE_VERSION: ${{ inputs.create_version }}
        run: |
          set -euo pipefail
          npm stage publish "$RUNNER_TEMP/npm-release/convax-marketplace-$MARKETPLACE_VERSION.tgz" --access public
          npm stage publish "$RUNNER_TEMP/npm-release/convax-marketplace-kit-$KIT_VERSION.tgz" --access public
          npm stage publish "$RUNNER_TEMP/npm-release/create-convax-marketplace-$CREATE_VERSION.tgz" --access public
      - uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: marketplace-npm-tarballs
          path: ${{ runner.temp }}/npm-release
          if-no-files-found: error
          retention-days: 7
