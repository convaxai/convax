{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "@convax/collaboration",
  "version": "0.1.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist",
    "protocol"
  ],
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    },
    "./migration": {
      "types": "./dist/migration.d.ts",
      "import": "./dist/migration.js",
      "default": "./dist/migration.js"
    }
  },
  "scripts": {
    "clean": "rm -rf dist",
    "build:js": "bun build --production --keep-names --target=browser --format=esm --packages=external --sourcemap=linked --root=src --outdir=dist src/index.ts src/migration.ts",
    "build:types": "tsc -p tsconfig.build.json",
    "build": "bun run typecheck && bun run clean && bun run build:js && bun run build:types",
    "pack:check": "bun run build && bun scripts/pack-check.ts",
    "test": "bun test",
    "typecheck": "tsgo --noEmit",
    "prepack": "bun run build",
    "prepublishOnly": "bun run typecheck && bun run test"
  },
  "dependencies": {
    "yjs": "13.6.31"
  },
  "devDependencies": {
    "@typescript/native-preview": "catalog:",
    "@types/bun": "catalog:",
    "typescript": "catalog:"
  },
  "publishConfig": {
    "access": "public"
  }
}
