export const projectFilePreviewScheme = "convax-project-preview" as const

export const projectFilePreviewPrivileges = {
  corsEnabled: true,
  secure: true,
  standard: true,
  stream: true,
  supportFetchAPI: true,
} as const
