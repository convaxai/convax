# Collaboration v10 Route B full-spec audit input

Status: exact-byte, inactive and untrusted audit input. This directory is not an
authority, implementation plan, compatibility source or R4 descendant.

The three architecture reviewers unanimously selected Route B after the exact R4
five-file bundle and an exact approved A–F delta proved unavailable. The files in
this directory are preserved only so every final normative clause can receive one
explicit disposition: retain with fresh proof, replace, or delete.

Rules:

- no clause inherits authority from R4, rejected R5.4 or an earlier vote;
- no current file under `authorities/collaboration-v10/r5` is active merely because
  it is byte-identical to this input;
- the final five-file authority must be standalone and use a new lineage, manifest,
  bundle/core/protocol identities and independent 3/3 exact-byte sign-off;
- approved design inputs A–I, R5.10–R5.14 and global URI governance are audit
  obligations, not automatically applicable patches;
- old collaboration bytes fail closed and may be removed only by the explicit,
  confirmed breaking reset. Ordinary Project files are outside that reset.

`audit-input.sha256` is the only identity list for this preserved input. It must
never be reused as an active authority manifest.
